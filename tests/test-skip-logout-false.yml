name: Test ECR Login - Skip Logout False (Default Cleanup)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: ecr-login
        uses: .
        with:
          registry-type: private
          skip-logout: 'false'
          mask-password: 'true'
        continue-on-error: true
        env:
          AWS_ACCESS_KEY_ID: ''
          AWS_SECRET_ACCESS_KEY: ''
          AWS_SESSION_TOKEN: ''
          AWS_DEFAULT_REGION: us-east-1

      - name: Assert action failed due to missing credentials (skip-logout false)
        env:
          OUTCOME: ${{ steps.ecr-login.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action correctly failed when no AWS credentials are provided (skip-logout=false path)"
          elif [ "$OUTCOME" = "success" ]; then
            echo "WARN: action succeeded unexpectedly (real AWS credentials may be present)"
            echo "PASS: treating as pass since action completed without error"
          else
            echo "FAIL: unexpected outcome: $OUTCOME"
            exit 1
          fi
